Support boolean const `where` predicates on items without generic parameters, such as `fn operation() where allowed(3) {}`. Each condition is evaluated at compile time where it is declared, even if the item is unused; a false condition or a failed evaluation is an error. Const predicates in generic items are rejected for now.
