Const functions can borrow their own locals and parameters with `mut` and `ref`, so helpers that update a value in place, or take a `mut` argument, now evaluate at compile time. Returning a borrow of a local from the function that owns it is rejected during evaluation.
