Fix `#[event]` structs with more than 16 non-indexed fields, which failed to compile because their data was encoded as a tuple and the tuple codecs stop at 16 elements. The data fields are now encoded directly as one ABI record, producing the same bytes as before for events that already compiled.
