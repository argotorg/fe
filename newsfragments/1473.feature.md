Added the `Abs` trait in `core::num`, bundling `abs`, `checked_abs`, `wrapping_abs`, `saturating_abs`, and `overflowing_abs` as `const fn` methods on all signed integer types — each taking a different stance on the asymmetric `T::MIN` edge case.
