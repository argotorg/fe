Fixed contract layout for omitted const defaults used by storage and transient effect handles. Nested `StorageMap`, `TSlot`, and `Mutex` fields now get distinct roots after their inline storage footprint and in the correct address space, preventing ordinary storage, enum payloads, and unrelated transient slots from aliasing each other.
