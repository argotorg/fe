Fixed a compiler error when a mutable `own` enum parameter, such as `mut _ value: own Option<u256>`, is matched with `if let` or `while let` and reassigned. Such functions previously failed to compile with an internal `SlotCarrierMismatch` error.
