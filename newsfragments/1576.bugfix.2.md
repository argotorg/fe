Evaluate explicit const arguments of method and associated function calls, such as `Window<1> {}.take<3>()`, against the parameter's type. Constant evaluation used to panic when such a function read its const parameter.
