Added the `UnsignedAbs` trait in `core::num`, exposing `T::unsigned_abs()` as a `const fn` on signed integers that returns the magnitude in the unsigned type of the same width — handling `T::MIN` without overflow.
