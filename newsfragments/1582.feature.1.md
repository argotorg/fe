Const functions can take immutable trait providers through `uses` and supply them with `with`, including forwarding through generic const functions and calls to const trait methods. Mutable effects, type-keyed (storage) effects and extern functions with effects are rejected.
