trait Sized {
    const SIZE: u256
}

struct Zero {}
impl Sized for Zero {
    const SIZE: u256 = 0
}

fn div_pred<T>() where T: Sized, 100 / T::SIZE > 0
{
}

fn call_div_zero() {
    div_pred<Zero>()
}
