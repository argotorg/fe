struct Slot<T, const ROOT: u256 = _> {}

trait T {
    fn f<const N: u256>(x: Slot<u256>, y: Slot<u256, N>)
}

struct S {}

impl T for S {
    fn f<const M: u256>(x: Slot<u256>, y: Slot<u256, M>) {}
}
