msg M {
    #[selector = 0]
    Ping -> (),
}

contract C {
    mut balances: StorageMap<u256, u256>

    recv M {
        Ping -> () uses (balances) {
            let ptr = balances.ptr(key: 1)
            ptr.write(2)
        }
    }
}
