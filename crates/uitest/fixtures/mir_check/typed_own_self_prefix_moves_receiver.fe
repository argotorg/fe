struct S {
    value: u8,
}

impl S {
    fn consume(own self: Self) -> u8 {
        self.value
    }
}

fn use_after_consume() -> u8 {
    let s = S { value: 1 }
    let value = s.consume()
    value + s.value
}
