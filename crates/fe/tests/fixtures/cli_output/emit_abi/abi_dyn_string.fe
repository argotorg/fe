use std::abi::DynString

msg FooMsg {
    #[selector = sol("set_name(string)")]
    SetName { name: DynString },
}

pub contract Foo {
    recv FooMsg {
        SetName { name } {
            let _ = name
        }
    }
}
