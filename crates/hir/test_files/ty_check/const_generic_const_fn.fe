extern {
    const fn a() -> usize
    const fn b() -> usize
    const fn foo() -> usize
}

struct Foo<const N: usize = a()> {}
type F = Foo<b()>

struct Transient{} // Marker type for transient address space

struct Map<K, V, const SLOT: usize = foo()> {}
type M = Map<u32, u32, foo()>
