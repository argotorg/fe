fn foo<T>(x: T) -> T
  uses (balance: u256)
{
    move x
}

struct Bar {
    inner: u256
}

impl Bar {
    fn id<T>(self, x: T) -> T {
        move x
    }
}

fn test() {
    let f = foo<u256>
    let m = Bar::id<u256>
    let _ = (move f, move m)
}
